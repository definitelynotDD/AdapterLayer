[phases.setup]
nixPkgs = ["tesseract", "python311", "python311Packages.pip", "gcc"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "python3 -m pip install -r Requirements.txt"]

[start]
cmd = "python3 start.py"